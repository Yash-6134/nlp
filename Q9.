# Q.9) Perform Named Entity Recognition (NER) on given text  (Smaller version)

import spacy

nlp = spacy.load("en_core_web_sm")


text = input("Enter the sentance: ") # Example: Barack Obama was born in Hawaii and served as the President of the United States.

doc = nlp(text)

print("\n--- NAMED ENTITY RECOGNITION ---")
for ent in doc.ents:
    print(ent.text, " --> ", ent.label_)
